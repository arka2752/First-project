#include <bits/stdc++.h>
using namespace std;
int main()
{
    int part, ropes;
    cin >> part;
    cin >> ropes;
    int a[part];
    for (int i = 1; i <= part; i++)
    {
        cin >> a[i];
    }
    int temp = 0;
    for (int i = 1; i <= ropes; i++)
    {
        int x, y;
        cin >> x;
        cin >> y;

        
    }
    cout << temp;
}